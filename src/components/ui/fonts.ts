import { Nunito,Gabarito } from 'next/font/google'

export const nunito= Nunito({
  subsets: ['latin'],
  display: 'swap',
})

export const gbarito=Gabarito({
  subsets: ['latin'],
  display: 'swap',
})